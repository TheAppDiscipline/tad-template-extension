# Env vars are build-time only in extensions (WXT bakes them into the bundle).
# Prefix with WXT_ to expose to application code.
# Use `wxt build --mode production` to pick up `.env.production`, etc.

# WXT_SIDECAR_URL=https://api.your-sidecar.example.com
